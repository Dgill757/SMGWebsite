SUMMIT OS - BLOCKERS (updated 2026-07-06 evening)
==================================================

BLOCKER 1: ANTHROPIC API CREDITS  [status checked below - see FINAL summary]
----------------------------------------------------------------------
If not yet done: console.anthropic.com -> Billing -> add $20-50 + AUTO-RELOAD.
Verify with: python test_demo_long.py

BLOCKER 2: SUPABASE RLS  [RESOLVED 2026-07-06]
----------------------------------------------------------------------
FIXED this session. What changed:
  - RLS enabled on all 10 unprotected tables (staged: hot_leads first, verified,
    then the rest) and the 13 wide-open anon policies on the other 6 tables
    were dropped. The anon key now sees ZERO rows and cannot write (verified).
  - The dashboard no longer talks to Supabase at all. It goes through new
    Railway /db/{table} endpoints (8-table allowlist) using the SERVICE key
    server-side.
  - The dashboard HTML now contains NO secrets: no anon key, no AVA API key,
    no hardcoded password, no GHL token. Login is verified SERVER-SIDE by
    Railway (env DASHBOARD_PASSWORDS, default "ava2026,summit2026" -
    change it anytime with:  railway variables --set DASHBOARD_PASSWORDS="newcode")
  - Fleet scripts daily_enrich_new.py + website_notifier.py switched from the
    hardcoded anon key to the service key from scripts\.env.
  - Record + re-runnable SQL: SUPABASE_RLS.sql

STILL RECOMMENDED (10 min): rotate the GHL private token if you haven't yet
(it was public in the dashboard HTML until earlier today). GHL -> Settings ->
Private Integrations -> Regenerate, then:
  railway variables --set GHL_PRIVATE_TOKEN="pit-NEW"
and update C:\Users\DanGi\outreach\.env + C:\Users\DanGi\scripts\.env.
Optionally also rotate the Supabase anon key (Settings -> API) - it is now
powerless, but rotation costs nothing since nothing uses it anymore.

BLOCKER 3: SEMGREP GUARDIAN PLUGIN NOT LOGGED IN  [BLOCKS CLAUDE FILE TOOLS]
-----------------------------------------------------------------------------
Still present. Run /mcp in an interactive Claude Code session to log in, or
disable the plugin. Sessions work around it via PowerShell, but slower.

